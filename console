#!/usr/bin/env php
<?php

use Core\App;
use Commands\ConsoleKernel;

require_once __DIR__ . '/Core/App.php'; // Подключение ядра

App::init(); // Инициализация (путь вычислится автоматически относительно Core)

$kernel = new ConsoleKernel(); // Создание консольного ядра
$kernel->handle($argv); // Обработка аргументов командной строки
