#!/usr/bin/php
<?php

// Подключаем App из /app/Core/
require_once __DIR__ . '/app/Core/App.php';

Core\App::init(); // Инициализация

// Запуск консольного ядра (оно тоже переехало в /app/Commands)
(new Commands\ConsoleKernel())->handle($argv);
